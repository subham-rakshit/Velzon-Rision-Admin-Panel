import React from "react";

export const metadata = {
  title: titlesObject.pagesMaintenance.title,
};

const PagesMaintenance = () => {
  return <div>PagesMaintenance</div>;
};

export default PagesMaintenance;
